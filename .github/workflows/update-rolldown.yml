name: Update Rolldown

permissions:
  contents: write
  pull-requests: write

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  update-rolldown:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          submodules: recursive
          persist-credentials: true

      - name: Get current submodule tag
        id: current
        working-directory: rolldown
        run: |
          tag=$(git describe --tags --exact-match)
          echo "tag=$tag" >> "$GITHUB_OUTPUT"
          echo "Current rolldown tag: $tag"

      - name: Get latest rolldown tag
        id: latest
        run: |
          tag=$(git ls-remote --tags --sort=-v:refname https://github.com/rolldown/rolldown.git \
            | grep -oP 'refs/tags/\Kv[0-9]+\.[0-9]+\.[0-9]+.*$' \
            | head -n 1)
          echo "tag=$tag" >> "$GITHUB_OUTPUT"
          echo "Latest rolldown tag: $tag"

      - name: Check if update is needed
        id: check
        run: |
          if [ "${{ steps.current.outputs.tag }}" = "${{ steps.latest.outputs.tag }}" ]; then
            echo "Already up to date (${{ steps.current.outputs.tag }})"
            echo "needed=false" >> "$GITHUB_OUTPUT"
          else
            echo "Update available: ${{ steps.current.outputs.tag }} -> ${{ steps.latest.outputs.tag }}"
            echo "needed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Update submodule to latest tag
        if: steps.check.outputs.needed == 'true'
        working-directory: rolldown
        run: |
          git fetch --tags
          git checkout "${{ steps.latest.outputs.tag }}"

      - name: Create pull request
        if: steps.check.outputs.needed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          branch="rolldown-update/${{ steps.latest.outputs.tag }}"
          title="chore: update rolldown to ${{ steps.latest.outputs.tag }}"

          # Check if a PR already exists for this branch
          existing=$(gh pr list --head "$branch" --json number --jq 'length')
          if [ "$existing" -gt 0 ]; then
            echo "PR already exists for $branch, skipping"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git checkout -b "$branch"
          git add rolldown
          git commit -m "$title"
          git push origin "$branch"

          gh pr create \
            --title "$title" \
            --body "Automated update of the rolldown submodule from \`${{ steps.current.outputs.tag }}\` to \`${{ steps.latest.outputs.tag }}\`." \
            --head "$branch"
